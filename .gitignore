# Local configuration (contains your APRS-IS passcode) - never commit
pktnet.conf

# The runtime database and generated certificates live under
# /var/lib/pktnet/ (outside this repo), so nothing here needs to ignore them.
# The certs/ folder in this repo is the versioned source data (users.db,
# users_base.csv, pktnet_radio.png, sample PDF, DB build scripts) and IS tracked.

# Python bytecode
__pycache__/
*.pyc
*.pyo

# Editor / OS cruft
*.swp
*~
.DS_Store
